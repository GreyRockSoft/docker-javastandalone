docker-javastandalone
=====================

A base Docker image that has Oracle's Java 8 installed with Ubuntu backing it.  To use in your own `Dockerfile` start with:

    FROM greyrocksoftware/javastandalone
